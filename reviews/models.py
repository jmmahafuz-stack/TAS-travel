from django.db import models

# Placeholder: no concrete models here to avoid creating migrations until needed.
# This file exists so the app can be imported during system checks.


class Placeholder(models.Model):
    class Meta:
        abstract = True
